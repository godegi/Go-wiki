<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>

    <% for(var i = 0; i < peopleNameList.length; i++){
      var uid = i;
      var name = peopleNameList[i]["name"];
      var description = peopleNameList[i]["description"]
      var url = encodeURI("/users?uid=" + uid); %>
      <div> <a href= <%=url%> > <%= peopleNameList[i]["name"] %> </a> </div>
    <% } %>

  </body>
</html>
